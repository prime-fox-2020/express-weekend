<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>SCHOOL APP WEEKEND</h1>
    <h3>Data Students</h3>
    <ul>
        <% for(let i = 0; i < students.length; i++) { %>
            <% for (key in students[i]) { %>
                <%= key + ': ' + students[i][key] %><br>
            <% } %>
            <br>
        <%} %>
    </ul>
</body>
</html>